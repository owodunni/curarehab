query ArticlesAndTerapeuts($filter: artiklar_filter) {
  artiklar(filter: $filter) {
    date_updated
    date_created
    etiketter
    locale
    omslagsbild {
      id
      title
    }
    sammanfattning
    slug
    titel
    user_created {
      first_name
      last_name
      work_title
      slug
      avatar {
        id
        title
        filename_download
      }
    }
  }
  terapeuter_directus_users {
    directus_users_id {
      first_name
      last_name
      work_title
      slug
      avatar {
        id
        title
        filename_download
      }
    }
  }
}
